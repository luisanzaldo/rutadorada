---
---

<dialog
    id="cannes-modal"
    class="bg-transparent m-auto p-0 backdrop:backdrop-blur-md backdrop:bg-black/80 w-full max-w-5xl rounded-2xl shadow-2xl border border-white/10 outline-none"
>
    <div class="bg-background-dark text-white max-h-[90vh] flex flex-col rounded-2xl overflow-hidden relative">
        <!-- Header -->
        <div class="flex items-start justify-between gap-4 px-6 sm:px-8 pt-6 pb-5 border-b border-white/10">
            <!-- Title block -->
            <div class="flex-1 min-w-0">
                <span class="inline-block text-[10px] font-bold uppercase tracking-[0.25em] text-primary mb-2">
                    79 Festival de Cannes
                </span>
                <h2 class="font-display font-bold text-2xl sm:text-3xl leading-tight">
                    Calificaciones <span class="text-primary">Cannes 2026</span>
                </h2>
                <p id="cannes-modal-updated" class="text-xs text-slate-400 mt-2 uppercase tracking-widest">
                    Cargando…
                </p>
            </div>

            <!-- Right column: buttons + sources -->
            <div class="flex flex-col items-end gap-3 flex-shrink-0">
                <div class="flex items-center gap-2">
                    <button
                        id="cannes-modal-share"
                        type="button"
                        class="bg-white/5 hover:bg-white/10 text-white rounded-full p-2 transition-colors border border-white/10"
                        aria-label="Copiar link"
                        title="Copiar link"
                    >
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <circle cx="18" cy="5" r="3"></circle>
                            <circle cx="6" cy="12" r="3"></circle>
                            <circle cx="18" cy="19" r="3"></circle>
                            <line x1="8.59" y1="13.51" x2="15.42" y2="17.49"></line>
                            <line x1="15.41" y1="6.51" x2="8.59" y2="10.49"></line>
                        </svg>
                    </button>
                    <button
                        id="cannes-modal-close"
                        type="button"
                        class="bg-white/5 hover:bg-white/10 text-white rounded-full p-2 transition-colors border border-white/10"
                        aria-label="Cerrar"
                    >
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <line x1="18" y1="6" x2="6" y2="18"></line>
                            <line x1="6" y1="6" x2="18" y2="18"></line>
                        </svg>
                    </button>
                </div>
                <div class="text-right">
                    <p class="text-[10px] uppercase tracking-[0.18em] text-slate-500 mb-1.5 font-bold">
                        Fuentes
                    </p>
                    <p class="text-xs text-slate-400 leading-tight">
                        <a
                            href="https://icsfilm.org/festivals/cannes/2026-cannes/cannes-2026-ics-press-industry-panel/"
                            target="_blank"
                            rel="noopener noreferrer"
                            class="hover:text-primary transition-colors underline-offset-2 hover:underline"
                        >ICS</a>
                        <span class="text-slate-600 mx-1">·</span>
                        <a
                            href="https://www.screendaily.com/cannes-jury-grid"
                            target="_blank"
                            rel="noopener noreferrer"
                            class="hover:text-primary transition-colors underline-offset-2 hover:underline"
                        >ScreenDaily</a>
                        <span class="text-slate-600 mx-1">·</span>
                        <a
                            href="https://www.caimanediciones.es/wp-content/uploads/2026/05/cuadro-critico_CANNES-2026.pdf"
                            target="_blank"
                            rel="noopener noreferrer"
                            class="hover:text-primary transition-colors underline-offset-2 hover:underline"
                        >Caiman</a>
                    </p>
                </div>
            </div>
        </div>

        <!-- Toast for share feedback -->
        <div
            id="cannes-share-toast"
            class="hidden absolute top-5 left-1/2 -translate-x-1/2 z-50 bg-primary text-background-dark text-xs font-bold py-2 px-4 rounded-full shadow-lg pointer-events-none"
        >
            Link copiado
        </div>

        <!-- Body -->
        <div class="flex-1 overflow-y-auto px-4 sm:px-8 py-6">
            <div id="cannes-modal-loading" class="text-center py-12 text-slate-400 text-sm">
                Cargando calificaciones…
            </div>
            <div id="cannes-modal-error" class="hidden text-center py-12 text-red-400 text-sm"></div>
            <div id="cannes-modal-empty" class="hidden text-center py-12 text-slate-400 text-sm">
                Aún no hay calificaciones cargadas.
            </div>
            <div id="cannes-modal-table-wrap" class="hidden overflow-x-auto rounded-xl border border-white/10">
                <table class="w-full text-sm">
                    <thead class="bg-card-dark">
                        <tr id="cannes-modal-thead" class="text-[10px] font-bold uppercase tracking-[0.18em] text-primary"></tr>
                    </thead>
                    <tbody id="cannes-modal-tbody" class="bg-background-dark"></tbody>
                </table>
            </div>
        </div>

        <!-- Footer -->
        <div class="px-6 sm:px-8 py-4 border-t border-white/10 text-[11px] text-slate-500 uppercase tracking-widest text-center">
            Ruta Dorada Films · Promedio de las críticas
        </div>
    </div>
</dialog>

<script>
    const CANNES_HASH = '#calificaciones-cannes-2026';

    function setupCannesModal() {
        const modal = document.getElementById('cannes-modal') as HTMLDialogElement | null;
        const trigger = document.getElementById('cannes-card-trigger');
        const closeBtn = document.getElementById('cannes-modal-close');
        const shareBtn = document.getElementById('cannes-modal-share');
        const shareToast = document.getElementById('cannes-share-toast');
        const loadingEl = document.getElementById('cannes-modal-loading');
        const errorEl = document.getElementById('cannes-modal-error');
        const emptyEl = document.getElementById('cannes-modal-empty');
        const tableWrap = document.getElementById('cannes-modal-table-wrap');
        const thead = document.getElementById('cannes-modal-thead');
        const tbody = document.getElementById('cannes-modal-tbody');
        const updatedEl = document.getElementById('cannes-modal-updated');

        if (!modal || !closeBtn) return;

        let loaded = false;
        let loading = false;

        function escapeHtml(s: string) {
            return String(s)
                .replace(/&/g, '&amp;')
                .replace(/</g, '&lt;')
                .replace(/>/g, '&gt;')
                .replace(/"/g, '&quot;')
                .replace(/'/g, '&#39;');
        }

        function showState(state: 'loading' | 'error' | 'empty' | 'table', message?: string) {
            loadingEl?.classList.toggle('hidden', state !== 'loading');
            errorEl?.classList.toggle('hidden', state !== 'error');
            emptyEl?.classList.toggle('hidden', state !== 'empty');
            tableWrap?.classList.toggle('hidden', state !== 'table');
            if (state === 'error' && errorEl && message) errorEl.textContent = message;
        }

        async function loadSnapshot() {
            if (loaded || loading) return;
            loading = true;
            showState('loading');
            try {
                const res = await fetch('/api/cannes/latest', { headers: { 'Accept': 'application/json' } });
                const data = await res.json().catch(() => ({}));
                if (!res.ok || !data.ok) {
                    showState('error', data.error || 'No se pudieron cargar las calificaciones.');
                    return;
                }
                const snapshot = data.snapshot as {
                    headers: string[];
                    rows: Array<Array<{ runs: Array<{ text: string; bold: boolean }> }>>;
                    updatedAt: string;
                };

                if (updatedEl) {
                    if (snapshot.updatedAt) {
                        const d = new Date(snapshot.updatedAt).toLocaleDateString('es-ES', {
                            year: 'numeric', month: 'long', day: 'numeric'
                        });
                        updatedEl.textContent = 'Actualizado · ' + d;
                    } else {
                        updatedEl.textContent = 'Sin datos aún';
                    }
                }

                if (!snapshot.headers.length || !snapshot.rows.length) {
                    showState('empty');
                    return;
                }

                function cellToHtml(cell: { runs?: Array<{ text: string; bold: boolean }> }) {
                    if (!cell || !cell.runs || cell.runs.length === 0) return '—';
                    const hasContent = cell.runs.some((r) => (r.text || '').trim() !== '');
                    if (!hasContent) return '—';
                    return cell.runs.map((r) => {
                        const t = escapeHtml(r.text || '');
                        return r.bold ? '<strong>' + t + '</strong>' : t;
                    }).join('');
                }

                if (thead) {
                    thead.innerHTML = snapshot.headers.map((h, i) => {
                        const headerCls = i === 0
                            ? 'text-left'
                            : i === 1
                                ? 'text-center text-primary bg-primary/8'
                                : 'text-center';
                        return '<th class="' + headerCls + ' py-3 px-3 sm:px-4 font-bold whitespace-nowrap">' + escapeHtml(h) + '</th>';
                    }).join('');
                }
                if (tbody) {
                    tbody.innerHTML = snapshot.rows.map((row, idx) => {
                        const stripe = idx % 2 === 0 ? 'bg-background-dark' : 'bg-card-dark/60';
                        return '<tr class="border-t border-white/5 ' + stripe + ' hover:bg-primary/5 transition-colors">' +
                            row.map((cell: any, i: number) => {
                                const cls = i === 0
                                    ? 'text-white font-normal'
                                    : i === 1
                                        ? 'text-center text-primary font-bold tabular-nums bg-primary/5'
                                        : 'text-center text-slate-200 tabular-nums font-medium';
                                return '<td class="py-3 px-3 sm:px-4 ' + cls + '">' + cellToHtml(cell) + '</td>';
                            }).join('') +
                            '</tr>';
                    }).join('');
                }
                showState('table');
                loaded = true;
            } catch (err) {
                showState('error', 'No pude conectar con el servidor.');
            } finally {
                loading = false;
            }
        }

        function openModal() {
            if (!modal.open) {
                modal.showModal();
                loadSnapshot();
            }
        }

        trigger?.addEventListener('click', (e) => {
            e.preventDefault();
            openModal();
        });

        closeBtn.addEventListener('click', () => modal.close());

        modal.addEventListener('click', (e: MouseEvent) => {
            const rect = modal.getBoundingClientRect();
            const inDialog = rect.top <= e.clientY && e.clientY <= rect.top + rect.height &&
                             rect.left <= e.clientX && e.clientX <= rect.left + rect.width;
            if (!inDialog) modal.close();
        });

        // Share button: Web Share API on mobile, clipboard fallback on desktop
        function getShareUrl() {
            return window.location.origin + window.location.pathname + CANNES_HASH;
        }

        function showToast(text: string) {
            if (!shareToast) return;
            shareToast.textContent = text;
            shareToast.classList.remove('hidden');
            window.setTimeout(() => shareToast.classList.add('hidden'), 2000);
        }

        async function copyToClipboard(url: string) {
            try {
                await navigator.clipboard.writeText(url);
                showToast('Link copiado');
            } catch (e) {
                // Fallback ultraconservador: input temporal + execCommand
                const tmp = document.createElement('input');
                tmp.value = url;
                tmp.style.position = 'fixed';
                tmp.style.opacity = '0';
                document.body.appendChild(tmp);
                tmp.select();
                try { document.execCommand('copy'); showToast('Link copiado'); }
                catch (err) { showToast('No se pudo copiar'); }
                tmp.remove();
            }
        }

        shareBtn?.addEventListener('click', async () => {
            await copyToClipboard(getShareUrl());
        });

        // Auto-abrir si el hash lo solicita
        function checkHashAutoOpen() {
            if (window.location.hash === CANNES_HASH) {
                openModal();
            }
        }
        checkHashAutoOpen();
        window.addEventListener('hashchange', checkHashAutoOpen);
    }

    document.addEventListener('DOMContentLoaded', setupCannesModal);
    document.addEventListener('astro:page-load', setupCannesModal);
</script>
